# 卡片外链图片本地化

## 现象：卡片里的图全黑

角色卡把立绘直接写成外部图床地址（`<img src="https://i.postimg.cc/xxx.png">`）时，
Web 端看到的是一块**纯黑**，而桌面端同一张卡显示正常。

两个因素叠加：

1. **卡片帧的 CSP 默认是 `img-src 'self' data: blob:`**（`src/web/http/cardFrame.ts`）。
   卡片来自 `ELECKOI_CARD_ORIGIN`，图床是第三个域，浏览器直接拦掉请求
   ——不是加载失败，是**请求根本没发出去**（已在真实浏览器里验证：白名单外
   的图，图床服务器收不到任何请求）。
2. 卡片的 CSS 常把图片容器的底色写成 `--bg: #1a1a1a` 配黑色渐变遮罩，
   加载不出来就呈现为一块黑，看起来像"卡片坏了"。

桌面端不受影响：它用 `srcDoc` 渲染，没有这层 CSP。

> 排查口诀：**图能单独打开 ≠ 卡片能显示**。先看卡片是不是跨源引用，
> 再看 CSP，最后才怀疑卡片本身。

## 怎么选：改一个变量

四种做法都在服务里实现了，**改 `ELECKOI_CARD_IMAGE_MODE` 一个变量就能切换**，
不需要动上游代码，也不需要额外跑脚本：

| `ELECKOI_CARD_IMAGE_MODE` | 做什么 | 你这边能看 | 导出给别人 | 数据去哪 | 要配什么 |
| --- | --- | --- | --- | --- | --- |
| `self-hosted`（默认） | 导入时把图传进你自己的图床 | ✅ | ✅ 别人也能取到 | 进你自己的服务器日志 | 图床三件套 + 白名单填图床域 |
| `local` | 导入时存到本地目录，由本服务在卡片源以 `/card-images/` 提供 | ✅ | ❌ 别人打不开 | 不出你的机器 | **什么都不用配**（默认落在数据卷里） |
| `inline` | 导入时内联成 `data:` URI | ✅ | ✅ 图随卡自带 | 不出你的机器 | **什么都不用配** |
| `third-party` | **放行任意 https 图床**、不搬运 | ✅ | ✅（对方不删图就行） | 随图片请求发给**任意域** | 不用配（可选黑名单） |
| `off` | 什么都不做 | ❌ 黑块 | — | 不出去 | — |

一句话选择：**要发卡给人 → `self-hosted`；只自己用 → `local`；只缺几个小图标 → `inline`；
不想为每张卡加白名单 → `third-party`（先读下面那段代价）。**

```
# docker/.env
ELECKOI_CARD_IMAGE_MODE=local        # 换成 self-hosted / inline / third-party / off
```

改完 `docker compose -f docker/compose.yml up -d` 重启即可，启动日志会打印当前做法：

```
卡片图片做法：local —— 导入时存到 /data/card-images，由本服务在卡片源 /card-images/ 提供
```

`local` 的图片默认落在数据卷里的 `/data/card-images`（零配置）；想放到别的大盘上，
给 `eleckoi-web` 加一条挂载并把属主改成 `10001:10001`。

`third-party` 不需要任何白名单——它直接放行**任意 https 图床**；`self-hosted` 需要把图床域
填进白名单（规则见附一）。两档的代价差别见下。

### ⚠️ `third-party` 放开之后会怎样

放开 `img-src` 就等于**承认卡片能把你的数据发出去**：卡片是作者写的 HTML/JS，
它拼一个 `https://任意域/<你的聊天内容>.png` 就是一次图片请求（GET），
`connect-src 'none'` 只挡 fetch/XHR，**挡不住 `<img>`**。数据会落进那个域的访问日志。

换句话说：选这一档，安全性取决于**"你导入的卡是否可信"**，而不是取决于配置。
这也正是它省事的原因——不用再为每张卡补白名单。

想两者兼得就用 `self-hosted`（搬进自己的图床）、`local` 或 `inline`（图不出你的机器）。

**黑名单**（`ELECKOI_CARD_IMAGE_BLOCKED_HOSTS`，主机名或后缀，逗号分隔）是**尽力而为**的过滤，
不是安全边界：CSP 只能"允许哪些源"，做不到"允许全部、排除某几个"，所以黑名单由卡片帧
在写入卡片 HTML 前把命中的地址替换成透明占位图，并对常见的动态插入兜底。
它挡得住普通卡片的静态引用（验收里用真实浏览器验证过：被屏蔽的域一次请求都收不到），
但**挡不住蓄意绕过的脚本**。

### ⚠️ 放行哪个域，就等于允许卡片把数据发给哪个域

卡片是作者写的 HTML/JS。放宽 `img-src` 之后，它就能拼一个
`https://你放行的域/<你的聊天内容>.png` 把数据**发出去**——这是图片请求，不是上传，
CSP 拦不住。所以：

- 放行**你自己控制**的域（自建图床）：数据落进你自己的访问日志，只有你看得到 → 可接受。
- 放行**第三方公共**图床（postimg.cc、catbox 之类）：数据落进一个你既不了解保存多久、
  也无法核实谁看得到的第三方手里 → 想清楚再放。
- 另外：**不要把应用本身的源填进去**（那等于允许卡片通过 GET 触发应用接口）。

正因如此，`ELECKOI_CARD_IMAGE_ORIGINS` 只接受纯源、不支持通配符，
填错了会在启动时直接报错而不是静默生效。

## 附一、白名单怎么填（`third-party` / `self-hosted` 需要）

```bash
# docker/.env
ELECKOI_CARD_IMAGE_ORIGINS=https://img.example.com
```

规则与报错都在 `.env.example` 里写明了，要点：

- 只接受 `scheme://host[:port]`；带路径、查询串、通配符都会被拒绝并**启动失败**。
- 逗号分隔可以填多个（例如同时放行图床和一个局域网相册站）。
- 服务启动时会打印生效的清单，改完记得重启容器。

## 附二、`self-hosted`：把自己的图床跑起来（Zipline）

图床不是"一个能放文件的目录"就够了——别人用你的图床时，需要能注册、能上传、
能翻自己的图、能复制链接。所以这里直接带一个 [Zipline](https://zipline.diced.sh)：

```bash
# 生成两个密钥，填进 docker/.env 的「自带图床」段（.env.example 里有逐项说明）
openssl rand -base64 24 | tr -dc A-Za-z0-9 | cut -c -24    # → ZIPLINE_DB_PASSWORD
openssl rand -base64 42 | tr -dc A-Za-z0-9 | cut -c -32    # → ZIPLINE_CORE_SECRET
$EDITOR docker/.env

docker compose -f docker/compose.yml --profile images up -d zipline-db zipline
```

配置全部走 compose 的 `environment` 逐项传入，**没有第二个 env 文件**：
好处是只有这几个变量会进图床容器，`ELECKOI_MASTER_KEY` 之类的凭据不会被第三方镜像读到。

数据（Postgres + 图片）落在宿主机目录，由 `docker/.env` 的 `ZIPLINE_DATA_DIR` 指定，
默认 `/var/lib/eleckoi-zipline`，建议指到你的大盘上；图床会长大，命名卷放数据不好管也不好单独备份。

它在 `images` profile 下，**不显式启用就不会启动**：平时那条
`docker compose -f docker/compose.yml up -d` 完全不受影响（已验证：默认路径解析出的
服务只有 `eleckoi-web`）。图床同时包含一个 Postgres 容器（Zipline 只支持 Postgres），
两个都限了内存。

### 配好之后要做的三件事

1. **Lucky 加一条子域分流**：`img.kidamita.top` → `http://192.168.5.254:8792`
   （与 `ai.` / `cards.` 同样的做法）。Zipline 需要的代理头已由
   `CORE_TRUST_PROXY=true` + `CORE_RETURN_HTTPS_URLS=true` 处理。
2. **打开 `https://img.kidamita.top:57789`**，首次进入是安装向导，创建超级管理员账号。
3. **确认注册是邀请码制**：示例配置是 `FEATURES_USER_REGISTRATION=false` +
   `INVITES_ENABLED=true`，也就是"没邀请码注册不了、有邀请码就能注册"。
   邀请码在后台「邀请码」页生成后发给人。**这两个开关很容易配反**
   （`FEATURES_USER_REGISTRATION=true` 会让任何人直接注册，邀请码形同虚设），
   原因写在 `docker/.env.example` 的「自带图床」段里。
   **别忘了给每个账号设配额**——单人配额没有环境变量，只能在后台「用户」页逐个设，
   否则公网放开后会被刷满磁盘。

### 让脚本能传图：生成 API 令牌

后台「账号设置」里生成一个 API 令牌（`ZIPLINE_CORE_SECRET` 参与加密，所以**换掉它
会让已发的令牌全部失效**）。令牌就是后面 `--upload-token` 的值。

## 附三、离线批量搬迁与批量导入工具

> **配了图床的话，导入卡片时会自动搬图，你什么都不用做。**
> 在 `docker/.env` 里配齐三个变量即可（缺一个就自动关闭，行为与以前一致）：
> `ELECKOI_IMAGE_PUBLIC_BASE`、`ELECKOI_IMAGE_UPLOAD_API`、`ELECKOI_IMAGE_UPLOAD_TOKEN`。
>
> 之后的流程是**同步**的：在浏览器里点「导入」→ 卡入库 → 界面右下角出现进度条
> 「正在把卡片图片搬到你的图床… 12/341」→ 图片下载、上传、卡里的引用改写完
> → 进度条走完并提示「卡片图片已搬好，共 N 张」→ 这时导入才算成功。
> **进度条走完之前，卡不会以"能用"的最终态出现在你面前**，所以不存在
> 「刚导入就进去聊天、图还在换」的中间态。
>
> 启动日志里会有一行「导入卡片时自动搬图：已启用 → …」告诉你它有没有生效。
>
> 可调的变量：
>
> | 变量 | 默认 | 作用 |
> | --- | --- | --- |
> | `ELECKOI_IMAGE_LOCALIZE_MODE` | `inline` | 同步搬运（导入等搬完）。改成 `background` 则导入立刻返回、后台搬 |
> | `ELECKOI_IMAGE_CONCURRENCY` | `4` | 同时搬几张。串行搬几百张要几分钟，并发是让同步搬运可用的关键 |
> | `ELECKOI_IMAGE_MAX_PER_IMPORT` | `2000` | 单次最多搬多少张，超出保留外链并记日志 |
> | `ELECKOI_IMAGE_MAX_BYTES` | 20 MB | 单图体积上限 |
> | `ELECKOI_IMAGE_TIME_BUDGET_MS` | 15 分钟 | 单次搬运的总时间预算 |
> | `ELECKOI_IMAGE_ALLOW_LOCAL` | `0` | 是否搬运本机地址（卡里写死的 localhost 一般是作者的开发服务器） |
>
> ⚠️ 同步搬运意味着**导入请求会等图片搬完**。反向代理的后端读超时必须够大
> （建议 ≥600 秒；见 `反向代理部署.md`）。一张几百张图的大卡在 4 路并发下约 1–2 分钟。
>
> 下面这个脚本仍然保留，用于**已有数据**的一次性批量搬迁（例如换图床、或补搬历史卡）
> 与离线场景——它是幂等的，重复跑不会重复搬。

### 离线脚本

工具：`docker/rewrite-card-images.mjs`。它扫描角色卡散落图片引用的所有位置
（正文内容、正则替换、设定项、变量配置、聊天背景），逐个下载、用**魔数**判定
是不是图片，是图片就搬到你的图床，然后把卡里的引用改成图床地址。

三种搬运方式，按你选的方案挑一种：

```bash
# A) 传进自建图床服务（方案 4，Zipline）
node docker/rewrite-card-images.mjs /vol2/docker/volumes/docker_eleckoi-data/_data \
  --public-base https://img.kidamita.top:57789 \
  --upload-api https://img.kidamita.top:57789 \
  --upload-token <API 令牌> \
  --apply

# B) 落到本地目录（方案 3，交给你自己的静态服务／内网相册）
node docker/rewrite-card-images.mjs /vol2/docker/volumes/docker_eleckoi-data/_data \
  --public-base http://192.168.5.254:8793 --upload-dir /srv/images --apply

# C) 内联成 data: URI（方案 2，不用图床，导出即自带）
node docker/rewrite-card-images.mjs /vol2/docker/volumes/docker_eleckoi-data/_data \
  --inline --apply
```

**内联（C）的代价要量过再决定**：base64 会让体积涨约 1/3，而且这些字符是**写进卡片数据**的，
每次渲染卡片都会随数据一起下发。实测一张卡里 8 张图（合计 3.4 MB）内联后，
那条记录从 1.9 万字符涨到 **474 万字符**。所以它默认只内联 256 KB 以下的图
（图标、徽标正好），超过的保持外链并在结尾列出来：

```bash
--inline-max-bytes 1048576     # 想放宽就调大，但卡片数据会同比变大
```


先不加 `--apply` 跑一次是 dry-run（只报告、不下载、不改库）。

参数与行为：

- 第一个参数可以是**单个库文件**、**某个租户目录**（`tenants/t_xxx`），也可以是
  **整个数据根目录**或 **`tenants/` 本身**（自动遍历 `*/db/eleckoi-common.sqlite3`）。
- `--public-base` 是图床**对外**的地址：既用于识别"这张图已经搬过了"，也用于
  重写引用。用 API 方式时**请用公网地址调 API**，理由见下。
- `--upload-folder` 可选，把图都传进某个文件夹（值是该文件夹的 ID）。
- `--inline` 与 `--inline-max-bytes`：内联模式的开关与单图上限（默认 256 KB）。
- `--max-bytes`（默认 20 MB）、`--timeout-ms`（默认 20 s）。
- **地址以图床返回的为准**：脚本取服务返回的文件路径、再套回 `--public-base`，
  所以从内网 IP 还是公网域名调 API，写进卡里的地址都是对的那个。
- **文件名按内容 sha256 前 32 位命名**：同一张图多处引用只存一份（API 方式下
  同一 URL 在一次运行里也只传一次）；已经是自己域名的引用直接跳过（幂等）。
- 判定为"不是图片"的地址（字体 CSS、XML 命名空间、网页）保留原样，
  本机地址（localhost）直接跳过；网络类失败会自动重试一次并单独列出。
- 写入在一个事务里完成；任一步失败会回滚，并留下改前备份。

### 执行时机：先停容器

SQLite 库在容器运行时处于 WAL 状态，**不要**对着正在写入的库改数据。
稳妥顺序：

```bash
docker compose -f docker/compose.yml stop
node docker/rewrite-card-images.mjs ... --apply
docker compose -f docker/compose.yml start
```

改完别忘了一、里的 `ELECKOI_CARD_IMAGE_ORIGINS`，否则改写后仍然被 CSP 拦。

## 实测记录（2026-09-16）

在一份真实租户库（14 张卡、5 个会话）上跑：扫描到 25 个外部 URL，判定为图片
16 个（含 4 个 CC 协议 SVG 徽标、1 个 2 MB GIF、7 张别人自建图床的头像），
9.1 MB 落盘，改写 3 条记录（含「破碎与重生」状态栏里的 4 张立绘）；
"不是图片"6 个、"直接跳过"3 个（本机地址、字体 CSS 等）。二次运行改写 0 条，
确认幂等。

改前那 4 张立绘在服务端都是 HTTP 200、能单独打开，但卡片里呈现全黑
——正是本文第一节描述的 CSP 拦截。

## 最后一步

`local` / `inline` 两种做法**到此就已经能显示了**（与卡片帧同源，或图随卡自带），
不需要配白名单。

`self-hosted` / `third-party` 还需要把对应的域填进白名单，否则图片照样被 CSP 拦：

```bash
# docker/.env
ELECKOI_CARD_IMAGE_MODE=self-hosted
ELECKOI_CARD_IMAGE_ORIGINS=https://img.kidamita.top:57789
```

然后 `docker compose -f docker/compose.yml up -d` 重建容器
（改 `.env` 不会自动进容器，必须重建），启动日志会打印生效的白名单。

## 多租户下的行为（同一张卡被多人导入）

问得最多的一个问题：**我导入了某张卡、图已经搬到我的图床，我朋友在他自己的账号下导入同一张卡，
会不会又搬一遍、图床上多一份？**

分两种情况看，取决于用哪种做法：

| 做法 | 第二个用户导入同一张卡 | 图床存储 |
| --- | --- | --- |
| `self-hosted` | **会复用**：按内容哈希（sha256）查共享索引，命中且图片还在就跳过上传，直接把已有地址写进他的卡 | 同一张图**只存一份** |
| `local` | 天然复用：本地目录按内容哈希命名，已存在就不重复写 | 只存一份 |
| `inline` | 图床不参与；每个用户的卡片数据各自内联一份 | 不占图床 |
| `third-party` | 不搬运，各人卡片仍指向原图床 | 不占你的图床 |

去重索引放在数据根（`<数据目录>/image-index.json`，所有租户共用一份），
命中后还会对地址发一个 `HEAD` 探活：**只有确认图还在才复用**——否则你在图床后台删了图，
卡片就会指向 404。探活拿不准时（网络抖动）宁可重传一份，也不让卡片裂图。

> 注意"同一张卡"的判定是**逐张图片按内容**，不是按卡文件名：
> 卡里那些真正相同的图（比如同一张头像用在多个位置）也只传一次。

几个仍然会重复的正常情况：

- 你在图床后台**删掉**了某些图 → 下次导入会重新上传（这是设计如此：宁可有图，不要裂图）。
- 同一个用户**重新导入同一张原卡**：卡文件里仍是原图床地址，所以会走一遍搬运；
  但内容索引命中后**不会重复占存储**，只是多一次探活。
- 换到**另一个图床**（改了 `ELECKOI_IMAGE_PUBLIC_BASE`）：索引按图床隔离，会在新图床重新搬一份。

⚠️ 还有一个和"重复"无关、但更值得注意的事：**所有租户的上传都用同一个图床令牌**，
所以图片在图表后台都算在**这个令牌所属账号**（也就是你）名下，占你的配额与磁盘。
朋友导得越多，你的图床涨得越快——建议在后台给每个邀请来的账号设配额，或定期清理。

关掉去重：`ELECKOI_IMAGE_DEDUPE=0`；改索引位置：`ELECKOI_IMAGE_INDEX_FILE=<路径>`。
